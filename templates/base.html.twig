<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>{% block title %}SnowTricks{% endblock %}</title>
    <link rel="icon" href="{{ asset('favicon.ico') }}">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block stylesheets %}
    {# Inclusion CSS individuelle si besoin #}
    {% endblock %}
</head>

<body>
    {# la navbarTop ne s'affiche qu'à partir de px, la navbarBottom prend le relais pour les plus petits affichages #}
    <nav id="main-nav-bar" class="navbar bg-dark border-bottom border-body d-none d-lg-block m-0 p-0" style="max-height:min-content "  data-bs-theme="dark">
        <div class="container-fluid m-0 p-0">
            <a class="navbar-brand px-5 py-0" href="{{ path("homepage") }}"><img id="main-logo" src="{{ asset('images/mainMenu/logoText.png') }}" alt="snowtricks logo">
            </a>
            <ul class="navbar-nav d-flex flex-row justify-content-end">
                <li class="nav-item">
                    <a class="nav-link active pe-2" aria-current="page" href="{{ path("homepage") }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link pe-2" href="#">Stricks</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link pe-2" href="{{ path("app_register") }}">Connexion</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link pe-2" href="#">Deconnexion</a>
                </li>
            </ul>
        </div>
    </nav>
    {% block head %}
    {% endblock %}
    <div class="container-fluid mb-5 pb-3"> {# container général de la page #}
        {% block body %}

        {% endblock %}

    </div>
    <footer>
        <nav id="footer-desk" class="navbar fixed-bottom bg-dark border-body d-none d-md-block" data-bs-theme="dark">
            <div class="container-fluid d-flex justify-content-center">
                <span class="navbar-brand m-0">Footer</span>
            </div>
        </nav>
        <nav id="footer-phone" class="navbar fixed-bottom bg-dark border-body d-block d-lg-none">

            <ul class="container-fluid navbar-nav d-flex flex-row justify-content-around py-3">
                <li class="nav-item"><a class="nav-link" href="{{ path("homepage") }}" title="Accueil"><i id="house-icon"
                            class="fa-solid fa-house p-2"></i></a></li>
                <li class="nav-item"><a class="nav-link" href="" title="Ajout Stricks"><i id="bookmark-icon"
                            class="fa-solid fa-bookmark p-2"></i></a></li>
                <li class="nav-item"><a class="nav-link" href="" title="Connexion"><i id="bracket-icon"
                            class="fa-solid fa-right-to-bracket p-2"></i></a></li>
                {# <li class="nav-item"><a class="nav-link" href="" title="Deconnexion"><i
                            class="fa-solid fa-right-from-bracket p-2"></i></a></li> #}
            </ul>

        </nav>
    </footer>
</body>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm"
    crossorigin="anonymous"></script>
<script src="https://kit.fontawesome.com/ea7977182c.js" crossorigin="anonymous"></script>

{# Inclusion des fichiers JS individuels des pages qui en ont besoin #}
{% block javascripts %}
{# Inclusion JS individuelle si besoin #}
{% endblock %}

</html>
